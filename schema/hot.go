// Code generated by SQLBoiler 3.6.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package schema

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// Hot is an object representing the database table.
type Hot struct {
	ID   int `boil:"id" json:"id" toml:"id" yaml:"id"`
	Hot  int `boil:"hot" json:"hot" toml:"hot" yaml:"hot"`
	View int `boil:"view" json:"view" toml:"view" yaml:"view"`

	R *hotR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L hotL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var HotColumns = struct {
	ID   string
	Hot  string
	View string
}{
	ID:   "id",
	Hot:  "hot",
	View: "view",
}

// Generated where

var HotWhere = struct {
	ID   whereHelperint
	Hot  whereHelperint
	View whereHelperint
}{
	ID:   whereHelperint{field: "\"hot\".\"id\""},
	Hot:  whereHelperint{field: "\"hot\".\"hot\""},
	View: whereHelperint{field: "\"hot\".\"view\""},
}

// HotRels is where relationship names are stored.
var HotRels = struct {
	Apps string
}{
	Apps: "Apps",
}

// hotR is where relationships are stored.
type hotR struct {
	Apps AppSlice
}

// NewStruct creates a new relationship struct
func (*hotR) NewStruct() *hotR {
	return &hotR{}
}

// hotL is where Load methods for each relationship are stored.
type hotL struct{}

var (
	hotAllColumns            = []string{"id", "hot", "view"}
	hotColumnsWithoutDefault = []string{}
	hotColumnsWithDefault    = []string{"id", "hot", "view"}
	hotPrimaryKeyColumns     = []string{"id"}
)

type (
	// HotSlice is an alias for a slice of pointers to Hot.
	// This should generally be used opposed to []Hot.
	HotSlice []*Hot

	hotQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	hotType                 = reflect.TypeOf(&Hot{})
	hotMapping              = queries.MakeStructMapping(hotType)
	hotPrimaryKeyMapping, _ = queries.BindMapping(hotType, hotMapping, hotPrimaryKeyColumns)
	hotInsertCacheMut       sync.RWMutex
	hotInsertCache          = make(map[string]insertCache)
	hotUpdateCacheMut       sync.RWMutex
	hotUpdateCache          = make(map[string]updateCache)
	hotUpsertCacheMut       sync.RWMutex
	hotUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single hot record from the query.
func (q hotQuery) One(exec boil.Executor) (*Hot, error) {
	o := &Hot{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "schema: failed to execute a one query for hot")
	}

	return o, nil
}

// All returns all Hot records from the query.
func (q hotQuery) All(exec boil.Executor) (HotSlice, error) {
	var o []*Hot

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "schema: failed to assign all query results to Hot slice")
	}

	return o, nil
}

// Count returns the count of all Hot records in the query.
func (q hotQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "schema: failed to count hot rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q hotQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "schema: failed to check if hot exists")
	}

	return count > 0, nil
}

// Apps retrieves all the app's Apps with an executor.
func (o *Hot) Apps(mods ...qm.QueryMod) appQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"app\".\"hot_id\"=?", o.ID),
	)

	query := Apps(queryMods...)
	queries.SetFrom(query.Query, "\"app\"")

	if len(queries.GetSelect(query.Query)) == 0 {
		queries.SetSelect(query.Query, []string{"\"app\".*"})
	}

	return query
}

// LoadApps allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (hotL) LoadApps(e boil.Executor, singular bool, maybeHot interface{}, mods queries.Applicator) error {
	var slice []*Hot
	var object *Hot

	if singular {
		object = maybeHot.(*Hot)
	} else {
		slice = *maybeHot.(*[]*Hot)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &hotR{}
		}
		args = append(args, object.ID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &hotR{}
			}

			for _, a := range args {
				if a == obj.ID {
					continue Outer
				}
			}

			args = append(args, obj.ID)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`app`), qm.WhereIn(`app.hot_id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load app")
	}

	var resultSlice []*App
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice app")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on app")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for app")
	}

	if singular {
		object.R.Apps = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &appR{}
			}
			foreign.R.Hot = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.HotID {
				local.R.Apps = append(local.R.Apps, foreign)
				if foreign.R == nil {
					foreign.R = &appR{}
				}
				foreign.R.Hot = local
				break
			}
		}
	}

	return nil
}

// AddApps adds the given related objects to the existing relationships
// of the hot, optionally inserting them as new records.
// Appends related to o.R.Apps.
// Sets related.R.Hot appropriately.
func (o *Hot) AddApps(exec boil.Executor, insert bool, related ...*App) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.HotID = o.ID
			if err = rel.Insert(exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"app\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"hot_id"}),
				strmangle.WhereClause("\"", "\"", 2, appPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.DebugMode {
				fmt.Fprintln(boil.DebugWriter, updateQuery)
				fmt.Fprintln(boil.DebugWriter, values)
			}
			if _, err = exec.Exec(updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.HotID = o.ID
		}
	}

	if o.R == nil {
		o.R = &hotR{
			Apps: related,
		}
	} else {
		o.R.Apps = append(o.R.Apps, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &appR{
				Hot: o,
			}
		} else {
			rel.R.Hot = o
		}
	}
	return nil
}

// Hots retrieves all the records using an executor.
func Hots(mods ...qm.QueryMod) hotQuery {
	mods = append(mods, qm.From("\"hot\""))
	return hotQuery{NewQuery(mods...)}
}

// FindHot retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindHot(exec boil.Executor, iD int, selectCols ...string) (*Hot, error) {
	hotObj := &Hot{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"hot\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, hotObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "schema: unable to select from hot")
	}

	return hotObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Hot) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("schema: no hot provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(hotColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	hotInsertCacheMut.RLock()
	cache, cached := hotInsertCache[key]
	hotInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			hotAllColumns,
			hotColumnsWithDefault,
			hotColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(hotType, hotMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(hotType, hotMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"hot\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"hot\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "schema: unable to insert into hot")
	}

	if !cached {
		hotInsertCacheMut.Lock()
		hotInsertCache[key] = cache
		hotInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the Hot.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Hot) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	hotUpdateCacheMut.RLock()
	cache, cached := hotUpdateCache[key]
	hotUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			hotAllColumns,
			hotPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("schema: unable to update hot, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"hot\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, hotPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(hotType, hotMapping, append(wl, hotPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to update hot row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "schema: failed to get rows affected by update for hot")
	}

	if !cached {
		hotUpdateCacheMut.Lock()
		hotUpdateCache[key] = cache
		hotUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q hotQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to update all for hot")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to retrieve rows affected for hot")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o HotSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("schema: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), hotPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"hot\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, hotPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to update all in hot slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to retrieve rows affected all in update all hot")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Hot) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("schema: no hot provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(hotColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	hotUpsertCacheMut.RLock()
	cache, cached := hotUpsertCache[key]
	hotUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			hotAllColumns,
			hotColumnsWithDefault,
			hotColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			hotAllColumns,
			hotPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("schema: unable to upsert hot, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(hotPrimaryKeyColumns))
			copy(conflict, hotPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"hot\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(hotType, hotMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(hotType, hotMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "schema: unable to upsert hot")
	}

	if !cached {
		hotUpsertCacheMut.Lock()
		hotUpsertCache[key] = cache
		hotUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single Hot record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Hot) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("schema: no Hot provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), hotPrimaryKeyMapping)
	sql := "DELETE FROM \"hot\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to delete from hot")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "schema: failed to get rows affected by delete for hot")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q hotQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("schema: no hotQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to delete all from hot")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "schema: failed to get rows affected by deleteall for hot")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o HotSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), hotPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"hot\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, hotPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "schema: unable to delete all from hot slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "schema: failed to get rows affected by deleteall for hot")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Hot) Reload(exec boil.Executor) error {
	ret, err := FindHot(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *HotSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := HotSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), hotPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"hot\".* FROM \"hot\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, hotPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "schema: unable to reload all in HotSlice")
	}

	*o = slice

	return nil
}

// HotExists checks if the Hot row exists.
func HotExists(exec boil.Executor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"hot\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "schema: unable to check if hot exists")
	}

	return exists, nil
}
